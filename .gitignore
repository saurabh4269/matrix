# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env

# Pytest
.pytest_cache/

# Heavy precomputed artefacts. Regenerable via `python -m src.precompute`.
# bm25_corpus.pkl in particular is ~194 MB.
data/bm25_corpus.pkl
data/embeddings.npy
data/dense_scores.npy
data/candidates.jsonl

# Filled labels — these are user-specific work.
# Keep eval/labels.csv (the empty template) tracked so others know the schema.
eval/labels_filled.csv
eval/labelled.jsonl

# Node / Vite
sandbox/frontend/node_modules/
sandbox/frontend/dist/
sandbox/frontend/.vite/

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/
